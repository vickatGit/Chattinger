<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chattinger">

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:name=".ui.ParentApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.Chattinger">
        <activity
            android:name=".ui.Activities.RoomCreationActivity.RoomCreationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.Activities.SplashActivity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.Activities.RoomsActivity.RoomsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.Activities.LoginActivity.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.Activities.Chat.ChatActivity"
            android:exported="false"></activity>
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
    </application>

</manifest>